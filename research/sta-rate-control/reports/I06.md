# I06 前置审计报告：公平调参与正式协议冻结

日期：2026-09-18

分支：`research/sta-rate-control`

开始 HEAD：`1d64163b14ee9c0788da291c824c448a291b1a69`

结论：**I06 blocked，未进入训练协议冻结、开发调参、选参验证或正式协议冻结；I07 不准入。**

## 1. 阻塞原因

I06 计划的显式依赖是“I05 通过”。实际 I05 在子门 A 失败：6/6 单轮飞行安全有效，但冻结的 45 个
性能检查仅通过 44 个；seed 6301 的 pitch 单轴 Proper-ISTA RMSE 比值为 1.32883，超过 1.25 门槛。
按照 A→B→C 的规则，I05 没有开放 B/C。

当前生产门控与报告一致：

- MODE=3 Proper-ISTA 只接受 AXES=1/3，AXES=7 明确拒绝；
- AXES=3 的能力代码存在，但没有通过 I05-A，所以不是合格开发基准；
- 最后通过的 Proper-ISTA 基准仍是 I04 的 AXES=1；
- Proper-ISTA 没有 yaw 执行器路径，也没有 DIV=2/4 飞行验收。

因此无法按用户指定内容完成两组公平比较：A 组没有 ESTA/原 ISTA/Proper-ISTA 的共同全轴有效域；B 组
不能给 PID/ESTA/原 ISTA/Proper-ISTA 相同的全轴候选评估。若让 Proper 的 yaw 静默使用 PID，或把 I06
改为 roll-only，再与其他全轴算法比较，会改变研究问题并造成算法混合，不是计划中的公平实验。包含 yaw
任务和 DIV=1/2/4 的正式矩阵也无法冻结为 I07 可执行协议。

## 2. 已读取的依据与历史数据边界

本次完整核对了新 TODO、原里程碑共同规则、历史进度表、I05、M10、ISTA-OPT01 和 ISTA-OPT02 报告，
并核对分支、HEAD、工作区和递归子模块。开始时工作区及子模块干净，本地与
`codev/research/sta-rate-control` 同步。

旧数据只能作为开发知识：

- M10 的新训练预算是 PID/ESTA/原 ISTA 各 12 次；正式种子 3101–3120 已公开、不可再作新留出集。
- OPT01 只给原 ISTA 追加 46 次开发尝试；OPT02 又包含 64 次诊断/ESTA 对照/原 ISTA 搜索。这些额外
  预算不平衡，不能称历史总体投入相等。
- I04/I05 的 Proper-ISTA 只有 roll 和 R/P 小幅开发数据，没有三轴/分频合格基准。
- I00–I05、M10、OPT01/02 的所有参数、种子与结果均已见，只能进入未来训练设计的先验披露，不能转成
  新正式留出或用于事后筛选正式结果。

## 3. 本阶段实际执行内容

只执行可复现的前置审计，没有修改生产源码、参数默认、模型、插件、保护或控制律。审计文件固定：

- I05 实际 `success=false`、尝试 6、单轮有效 6、失败项 `6301:pitch_only:1`；
- MODE=3 的代码门控只接受 AXES=1/3；
- 没有未提交的 `src/msg/boards/ROMFS/sitl` 生产改动；
- 被审计 HEAD 是当前提交历史的祖先，分支正确。

执行命令：

```bash
python3 research/sta-rate-control/ista_redesign/i06/check_preconditions.py
git diff --check
```

前置脚本实际执行 8 个非零断言，全部通过；“通过”仅表示阻塞事实可复现，不表示 I06 验收通过。
没有运行 C++ 单元测试或 SITL 构建，因为没有控制代码变化；没有启动 Gazebo。训练、验证、正式留出均为
0 次，未分配新种子，也没有外部大型实验目录或 ULog。

## 4. 未交付项

以下均未完成，不能填写或伪造：

- A 组共同有效域、共同参数和理论适用范围的最终冻结；
- B 组四算法等额新候选预算、参数范围、筛选与早停规则；
- 训练/选参验证运行及冻结参数；
- 20 个候选正式配对种子、独立性/样本量依据；
- 任务、扰动、惯量、噪声、分频的准确正式矩阵和逐轮清单；
- 指标权重、失败惩罚、缺失处理、配对置信区间、多重比较、TV/频谱/成本最终规范；
- 模型、插件、源码和参数组成的 I07 正式冻结点。

因为尚无共同可执行控制器集合，提前分配正式种子或撰写看似完整的协议会产生泄漏/返工风险。本阶段也
没有运行任何正式留出试验。

## 5. 解除阻塞所需的独立授权

后续应先建立 **I05 remediation**，而不是直接继续 I06：

1. 保留 I05-A 阴性结果，预先冻结 pitch 方差/标定/参数诊断，不把重跑称为原 A 的补飞；
2. 获得新的、独立开发种子并通过新的 R/P 准入门；
3. 再依次开放和验收 AXES=7，以及 Proper-ISTA 的 DIV=1→2→4；
4. 只有上述基准通过，才能重新启动 I06，先提交等预算训练协议，再运行训练/验证并冻结 I07 协议。

这需要用户单独授权，因为它会修改 I05 的控制能力和新增开发预算。当前不得执行 I07、不得把本报告当
正式协议、不 push、不实机部署。
