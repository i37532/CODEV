# I05 R-C 日志规范修订与剩余预算安排

起点：`0ba729be3c14fd9ea7cb94eece5d9b06af19cdaa`，研究分支，工作区干净。
本轮交付是可审查协议及清单，未开始新仿真或第1轮修订重分析，未改变原结果。

- 新协议：`ista_redesign/i05_remediation/RC_CONTINUATION_LOGGING_V1_CN.md`。
- 准确清单：`ista_redesign/i05_remediation/RC_CONTINUATION_LEDGER.json`。
- 可复现准备工具：`prepare_rc_continuation.py --output <不存在的文件>`，校验原jobs/progress指纹，
  只提取未执行索引1–17，并为每项保留原job内容指纹；不调用启动器。

完整状态序列仍严格验收；执行器主题改为部分记录的一致性证据，覆盖率如实披露。保留安全、性能、
参数、更新时序和ULog写入dropout标准；所有匹配力矩/推力必须一致，空交集、无窗口覆盖或异常
时间戳失败。该修订只适用于明确标识的新分析，旧formal03失败不被覆盖。

预算：原18次中的1次已执行，17次待执行，新增预算0。DIV1剩5次，DIV2与4各6次，种子7001–7003，
顺序不变。先完整离线重分析第1轮，通过修订门槛后才开始续跑；结果呈现原判定与修订判定两列。
任何任务或子门失败立即停止。最终只能称修订后的开发验收，不能称无偏离的预注册通过。

机器清单核验通过：原两个文件SHA相符、17索引唯一连续、5/6/6分组正确、第0项未重排为待飞、
修订第1轮结果保持null。准备命令与核验退出码均0，diff检查通过。无需控制器构建，本轮无生产变更。

执行准备尚未完成：需按协议实现显式修订分析及17轮续跑入口，测试拒绝与停止逻辑，绑定干净提交，
核对原轮与续跑固件语义/环境一致，再分析原第1轮并按预算继续。I05/I06验收状态本轮不变。
