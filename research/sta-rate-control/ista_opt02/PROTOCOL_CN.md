# ISTA-OPT02：地面状态诊断与低lambda2局部搜索

2026-09-18，用户明确要求先查原因，再围绕安全范围搜索。起点749f03cdad751132dbd694ff00187bd7126fbd9a。只限Iris SITL，不修改飞控、land detector、保护、mixer、模型、外环、输入增益g，不push，不改日常默认参数，不覆盖M10/OPT01。

## 先验诊断和待检验假说

对OPT01既有C01/C02/C03的只读解码发现：landed/maybe_landed在解锁约1s后解除，真实高度在约4.2s前几乎不变；nu在此期间积分，lambda2增大会放大积累。pitch设定值在离地后反号，积累的负nu阻碍正向纠偏。现有软件按land状态冻结是正确执行，但这些状态不保证物理离地；mixer饱和标志也不描述地面接触约束。

只读诊断以真实高度超过解锁前基准2cm作为离地邻域事件（不是精确触地时刻），另统计高度差<2mm且|vz|<.02m/s的静止地面阶段。记录release、4s、2cm及失败前的s/nu/输出/限制；用独立double二分核对理想ISTA候选、核对冻结状态和日志连续性。真值只用于离线诊断，不输入控制器。

新增分轴干预8次：inertia场景，seed5101/5102，四套配置随机配对顺序：D0原A增益，D1只把pitch lambda2×4，D2只把roll/yaw lambda2×4，D3三轴lambda2×4。lambda1固定[2.2,2.4,1.5]。保留所有失败，检查能否将pitch地面偏置/失稳与其他轴作用区分。若D0不通过、存在方程/时间/日志质量问题或观察不支持地面积累，应暂停搜索重新诊断，不把未解释的失效当作参数问题。

这不进行“真值离地冻结”的固件消融，因用户请求是诊断与参数搜索而非修改保护；对接触力/外环的因果归因保留范围限制。降落结论仅来自完成飞行，起飞中止不称为降落测试失败。

## 局部搜索预注册（新数据前冻结）

采用历史12/12通过的C01作为经验起点，不宣称整个参数区间已证明安全。只降低/保持lambda2，lambda1作小幅分轴调整。六个lambda1配置：

| profile | R | P | Y |
| --- | ---: | ---: | ---: |
| base | 2.2 | 2.4 | 1.5 |
| pitch1 | 2.2 | 2.8 | 1.5 |
| pitch2 | 2.2 | 3.2 | 1.5 |
| rp | 2.6 | 2.8 | 1.5 |
| py | 2.2 | 2.8 | 1.8 |
| rpy | 2.6 | 3.2 | 1.8 |

每组lambda2分别取[.05,.08,.02]×0.5、×1，形成12个固定候选L00…L11，不在看到成绩后改名单。g、nu上限3、命令上限.15、倾角15°、高度/模式/故障规则和原M10场景均不变。先在独立速率对象逐候选核对有限、有界输出（seed5001），只作为数值门槛，不据此宣称起降安全或再次选参。

- 训练seed5201/5202：ESTA原M10 B冻结参数六场景×两种子=12次。
- 筛查：每候选inertia/div4、seed5201各一次，共24次。任何控制/分析失败淘汰该候选；通过者按两场景主RMSE比值几何均值取前两名。
- 补充训练：两名各补其余10次（复用已飞两次，不补飞/重复计样本），最多20次，训练总上限56次。
- 全训练要求：每场景主RMSE比值≤1.10、每场景每轴≤1.25、全部飞行/分析接受。符合者按至少1%改善场景数降序、整体几何比值升序、ID顺序选一个。改善场景不足4/6即停止本轮，不以增加预算追求胜出。
- 达标后只验证该候选一次，seed5301/5302/5303，六场景×两个算法=36次。同一门槛，不按验证结果再换候选。不运行20种子的正式论文留出；若通过也仅是3种子初步独立验证。
- 总上限8诊断+56训练+36验证=100次Gazebo尝试。lambda1/lambda2之外无参数调度或阶段特化。训练固定顺序，诊断/验证同scene/seed内用PCG64固定随机顺序。

旧M10与OPT01为公开开发知识，不重新当未见测试。所有新种子与旧轮次分开；相同seed只控制原M10 IMU/力矩相位，不声称其他随机源也受控。失败保留在分母，失败RMSE不插补。TV、RMS、饱和、成本和各轴指标一并报告，不把跟踪更优等同全部指标更优。

## 执行与停止

先提交协议/工具、构建该干净提交后依次执行diagnosis、search。复用原run_m10/analyze_m10/batch_m10，无原飞控行为修改。每阶段不可变清单、源码/二进制/插件指纹一致；原始目录 `/home/yr/Desktop/codev doc/experiments/ISTA-OPT02-20260918`。

控制边界失败留存后继续固定清单；基础设施/数据质量异常暂停、不自行豁免；既有特批不适用。无重复飞行、无放宽阈值、无旧目录写入。完成后单独提交报告/证据并更新外部进度。若需修改land detector、冻结规则或更大预算，应另经用户同意并做独立消融，不能混入本轮参数比较。
