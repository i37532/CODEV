# 第 101 轮协议偏离与用户授权

用户在阻塞诊断后明确授权：“允许将该轮保持无效、不补飞，记录协议偏离后继续剩余 499 次”。原始阻塞记录 FORMAL_BLOCKER_101_CN.md 保留为历史证据。

对象仅为 `formal01/0100_B_m0_torque_s3101`：飞行完成，但启动前诊断序号 1633→1637 缺失 3 个样本（一个间隔），原分析仍判数据质量无效。冻结的自动复核脚本仅允许缺 1 个样本，本例确实未通过该脚本。本次是人工授权的协议偏离，不是自动规则验收通过，也不是离线修复成功。

该轮不补飞、不纳入有效 TV/频谱或完整接受结果；保留原始 ULog、分析、执行记录及指纹。通过同名 adjudication.json 记录特例后，从第 102 轮继续其余 499 个原定任务。源码仍为 f16ef80b3d0ebfca12b5820a14ddd7271e612c66，参数、种子、指标、窗口、任务顺序不变。

本授权不自动覆盖未来新的超规则阻塞。最终论文报告必须披露此次偏离，以及实际有效样本和配对数量；不能宣称 600 个有效样本。
