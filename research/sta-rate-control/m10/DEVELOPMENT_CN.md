# M10 开发/训练记录（不当作正式测试集）

起点585310ee7d，研究分支、工作区干净；M09的809份指纹全量校验退出0。
当前数据根 `/home/yr/Desktop/codev doc/experiments/M10-20260917`。
本文件随开发更新；正式冻结/结果SHA最终记入外部进度表。

## 已发生的故障和修正

1. `plugins01`：IMU独立构建缺Gazebo消息protobuf头目录，退出1。原编译日志保留。
   加与上游构建一致的 `/usr/include/gazebo-11/gazebo/msgs` 后，`plugins02` 两插件构建通过。
   只生成外部副本，不改上游源、子模块或系统库。
2. 一次启动batch命令未设置PYTHONPATH，numpy导入失败，尚未创建飞行目录/启动模拟器。
   文档给出既有本地依赖路径，没有重新下载依赖。
3. `smoke01`：PID起飞、60.500s悬停、降落上锁，原M09分析与新增seed/力矩检查通过。
   requested speed=5，真实回调250Hz，diagnostic序列完整；这不是正式测试样本。
4. `train01/0002_train_m2_nominal_s1101`：ISTA候选0(.85倍)起飞后在线检测倾角20.7692°>15°，
   中止自有SITL、还原参数，ULog/失败保留。没有扩大门槛、回退PID或重飞替换。候选按预定规则不合格。
5. `train01/0008_train_m2_nominal_s1101`：飞行完成，但旧M09分析器用`0<=elapsed<=36`，
   起始elapsed=0重复消费一次，span=36.004s，TV固定36s分母检查正确拒绝，批次停止。
   新M10可选sensor半开窗口保留M09历史默认路径；新增重复边界单测。
   所有8轮已成功飞行离线重算，旧JSON/NPZ等保存在`analysis_before_sensor_window_v2`。
   每轮adjudication记录原始文件指纹和新分析SHA；无重飞/无删除失败，显式核对后续跑原36轮清单。
6. 开发前9轮部分运行器元数据沿用了M00的`pid_parameters_modified=false`，但M10确实显式加载PID增益。
   参数JSON、CLI和ULog记录正确；后续将该元数据设为true，不修改旧记录。正式冻结版不含此误标。
7. `train01/0010_train_m1_nominal_s1102`：起飞命令后的立即CLI轮询读到相同publish_seq，
   宿主沿用的严格“任意两次读取不得相同”检查误判stale；ULog未记录控制器fault/abort。
   M10改用既有sim时间年龄检查与wall超时，允许同一lockstep瞬间的重复读取，不改PX4故障保护。
   该失败保留、计入已消耗训练预算，不冒称ESTA控制器数值故障；正式批次会把该类基础设施问题单列并停止。
8. `train01/0011_train_m2_nominal_s1102`：ISTA候选0再次超过15°，在线22.9087°，真实控制边界失败，保留。
9. 一次手工全Python回归遗漏 `M00_PASSING_RUN`，旧M00夹具初始化失败；46项实际运行、1项setUpClass错误，
   随后18项日常脚本通过。正式冻结前用包含该环境变量的verify入口重跑，不能用同一shell最后的0码冒充前一命令通过。

## 检查

- `verification01`：90个唯一C++用例，65个Python（当时新增M10的8项），全通过；构建/独立参考/原控制数学零改动检查退出0。
- 随后统计区间和重复窗口单测新增，目前M10共10项通过。最终冻结前会重新核对全部用例数量。
- `scalar01.json`：60个独立binary64三轴简化对象场景通过，含3候选×3算法×6条件及A组ISTA6条件。
  25ms电机一阶滞后、物理力矩/惯量和噪声；它不执行生产保护/滤波，不能替代真实C++与Gazebo检查。
- 已完成的同seed1101名义训练，前16组真实IMU增量最大差0，证明研究seed入口真实生效；
  不代表GPS/mag/baro也独立播种或整条闭环轨迹逐位相同。
- 初批力矩插件触发相对控制器激励起点约.204～.212s，记录实际时刻并用于恢复指标；
  小于预设.5s同步边界，不伪称零延迟。所有场景都有该插件，nominal实际施力严格为0。

未进行实机、MATLAB、板载CPU/WCET、8字轨迹或大姿态论文验证。

## 等预算训练完成

train01实际36轮，每模式12次尝试：PID12/12、ESTA11/12、ISTA8/12完整接受，共31/36。
ISTA候选0四轮均起飞倾角越界，其中nominal/torque各两个seed；ESTA候选0有上述宿主轮询误报。
失败均消耗预设预算且候选不合格，因此ESTA低增益候选只有3个有效性能观测，
不能声称已比较了该候选全部4轮真实性能。所有被选候选都有完整4轮有效训练。

选中PID候选2（P/I/D×1.15，训练主指标.0049669374rad/s）、ESTA候选1（.0039033782）、
ISTA候选1（.0039554273）。A组仍使用共同M06增益，不能把ISTA的工程pitch2.0带入A组。
相同训练预算是12次实际尝试/算法、3个预设候选，不是全参数空间最优或无基础设施影响的优化保证。
完整选择数值见外部train01/selected.json，正式冻结时纳入FROZEN.json。

绘图库本机未安装，plot_dependencies01通过固定版本wheel安装到独立外部目录；
没有覆盖系统或PX4 Python。安装命令、退出0与完整包版本保存。

先导期间进一步审查元数据：早期m04_protocol.json继承了M08的旧1.25比值说明，
但M10实际分析从未执行该门槛；正式版改为明确M10规范引用并删除旧比值字段。
在惯量场景开始前补齐include的GPS独立link惯量副本与张量正定/三角不等式检查，
质量及原模型均不改；此前名义场景scale=1，原GPS物理参数与副本相同。
独立简化对象的初版scalar01把力矩波形直接作为自己的标量轴输入，未转FLU→FRD；
后续scalar02加入diag(1,-1,-1)后重跑。该修订不改Gazebo实际施力或飞控公式，旧结果保留。

## 先导中的真实边界失败

`pilot01/0062_B_m0_div2_s2101` 完成60.420s悬停及降落上锁，但全飞行roll指令峰值
0.1533712745超过预设.15边界。峰值在sensor时间111.084s，降落命令103.412s之后、
上锁115.296s之前；不是36s跟踪窗口内的失稳。没有fault/abort或ULog dropout。
仍按控制边界失败计数，不缩窄验收窗口、不提高门槛、不把“飞完”写成“全部通过”。
研究ULog `09_31_29.ulg` 的SHA-256为
`c3b3c1eb14399270de911249251b21a1bee8a6a4b0dc19f78bfc4c6956485d26`。
离线复算器保留这类有效失败并继续审计后续日志；只有基础设施/数据有效性问题仍停止。
冻结参数不因这次先导结果重新选择。

`pilot01/0080_A_m1_div4_s2102` 在CLI返回后才发送触发文件，实际到达比控制激励晚.580s，
超过预定.5s；飞行成功但数据有效性拒绝，批次按规则停止（81/90已尝试，79接受、1控制边界失败、
1同步失败、9未运行）。失败ULog SHA-256为
`8708fa016cdf3bbe41665d1b043200391be1e50161eb27266fefbfec5d65f5ff`。
没有篡改旧同步时间或将其改判通过。插件03改为读取控制器sample减elapsed的显式起点，
必须在波形2s零前缀内送达；离线检查传送延迟及实际epoch对齐。原plugins02及81条数据全部保留。
pilot02第一轮又发现param set的CLI应答先于姿态环消费参数，此时research_elapsed仍为-1，
新检查正确拒绝、保留失败并还原参数。加入有界等待，直到实际诊断显示激励有效再读取起点；
不把CLI成功当成激励已经生效。改用pilot03重新进行全90条工具验证，参数选择不变，正式种子未用。
该开发修订发生在正式冻结之前。

pilot03第38条 `0037_B_m0_inertia_s2102` 完成起降，但roll峰值.153417>.15，仍为边界失败。
第67条 `0066_A_m2_div2_s2102` 完成起降，在未解锁的8.948→8.956s间publish_seq2192→2194，
恰缺一条诊断样本，其他sensor间隔均4ms、飞行期完整、fault/abort/dropout均0。
该轮保持数据质量失败、不产出有效TV/PSD；不是把丢样补齐后称为通过。
留下含原始ULog/分析SHA的accepted=false审查记录后继续原90条清单，不重飞替换。
该规则在正式冻结前写入规范；其他基础设施/数据问题仍须停止检查。

恢复时间的离线计算另修正浮点ULP边界：使用日志整数微秒，避免恰好停止的样本因浮点差被跳过4ms。
保持1s、阈值.02rad/s、观察10s均不改；新增精确边界单测，最终复算保留此前分析文件。

## 冻结前完成状态

- `verification04`：90个唯一C++、74个Python（研究56、日常18）实际执行并通过；
  包含17个M10用例，6011个独立参考样本及构建/旧PID/ESTA/ISTA回归，命令退出0。
- `scalar02.json`：修正坐标约定后的60个独立简化对象检查通过，不当生产控制器或Gazebo证明。
- 最终先导`pilot03`：90/90执行、90次均完成起降，88次满足完整接受条件；上述1个控制边界失败、
  1个保留的数据质量失败。没有重飞补齐到90成功。正式seed3101..3120尚未使用。
- 用最终分析器对可分析先导逐轮离线复算，原结果归档在`analysis_before_freeze_v1`，
  无效丢样轮保持无效、不补TV/PSD。最终汇总见本目录`pilot_summary`，不能将执行时旧summary混用。
- 已生成`FROZEN.json`，保留训练选择、完整非实验参数及先导精度估计；不是以先导分数再选增益。
  正式计划仍600次、每场景20个种子；样本量是可行性预算，不声称已证明统计功效。
- 所有原模型、飞控算法/消息/滤波、项目启动器及子模块均未改；全部实际运行限定Iris SITL。

最终有效先导649项非实验参数一致；同seed前5000个gyro增量最大差0，跨seed最大绝对Pearson相关
.027289（描述性诊断，不是IID证明）。激励时间起点最大偏差.008s，文件传送延迟最大.432s，
均在预定边界内。88个有效运行最大倾角2.33104°、高度误差.127827m；原motor话题每轮缺
1182～1667个样本，不能当无损谱。先导2或3个完整配对估计的n20正态近似半宽范围
1.15074e-6～1.63059e-4rad/s；小样本不确定性大，不作为功效保证或调整正式样本数的依据。
